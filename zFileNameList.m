% zFileNameList specifies the PDB files to be loaded.  Several programs
% call this, so you only need to change the list in one place.

function [Filenames] = zFileNameList;

Filenames = {'1j5e' ...
             };  % add additional filenames here


